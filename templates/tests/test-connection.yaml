apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "learn-helm.fullname" . }}-test-connection"
  labels:
    {{- include "learn-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "learn-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
